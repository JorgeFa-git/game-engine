package game.window;

import core.window.Window;

public class SnakeWindow extends Window {
    public SnakeWindow(int windowWidth, int windowHeight, String windowTitle) {
        super(windowWidth, windowHeight, windowTitle, true);
    }
}
